//
//  BTC_MarketStatsCell.swift
//  GoPaxTest
//
//  Created by frank on 2020/10/19.
//
/*
import UIKit
///`Market Price Statistics Cell (BTC)`
class BTC_MarketStatsCell: UITableViewCell {
    @IBOutlet weak var name: UILabel!
    @IBOutlet weak var volume: UILabel!
    @IBOutlet weak var price: UILabel!
    //MARK: - awakeFromNib
    override func awakeFromNib() {
        super.awakeFromNib()
        // Initialization code
    }
    //MARK: - setSelected
    override func setSelected(_ selected: Bool, animated: Bool) {
        super.setSelected(selected, animated: animated)
    }
    /// `Load Cell with Entity`
    /// - Parameter mockData: Entity
    func configure(data: Entity) {
        self.name.text = data.quoteAsset
        self.volume.text = String(describing: data.volume.rounded(decimal: 3))
        self.price.text = String(describing: data.price.rounded(decimal: 3))
    }
    /// `Load Cell with DummyData`
    /// - Parameter mockData: MockData
    func configure(mockData: MockData) {
        self.name.text = mockData.name
        self.price.text = String(describing: mockData.price)
        self.volume.text = String(describing: mockData.volume)
    }
}
*/
